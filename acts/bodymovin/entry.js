bodymovin.loadAnimation({
  container: document.body, // the dom element that will contain the animation
  renderer: 'svg',
  loop: true,
  autoplay: true,
  path: 'lego_loader.json' // the path to the animation json
});